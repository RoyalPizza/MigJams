namespace Pizza.CharacterControl
{
    /// <summary>
    /// An enum representing the method of transport being used for movment.
    /// </summary>
    public enum MovementTransport { Normal, Surf, Ride }

    /// <summary>
    /// An enum representing the speed the player is moving.
    /// </summary>
    public enum MovementSpeed { Idle, Slow, Fast }

    /// <summary>
    /// An enum representing the direction the player is facing.
    /// </summary>
    public enum MovementDirection { Up, Right, Down, Left }
}